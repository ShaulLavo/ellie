{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "test": "bun test",
    "check-types": "tsgo --noEmit"
  },
  "dependencies": {
    "@ellie/db": "workspace:*",
    "@ellie/env": "workspace:*",
    "@ellie/durable-streams": "workspace:*",
    "@ellie/router": "workspace:*",
    "@ellie/rpc": "workspace:*",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "bun-types": "latest"
  },
  "module": "src/server.ts"
}
