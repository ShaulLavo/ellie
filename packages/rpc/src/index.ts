// Shared types — importable from both server and client
export type {
  InferSchema,
  ExtractParams,
  HasParams,
  CollectionDef,
  StreamDef,
  ProcedureDef,
  Router,
  RouterDef,
  SubscriptionHandle,
  CollectionClient,
  StreamClient,
  ProcedureClient,
  RpcClient,
} from "./types"
