// Shared types — importable from both server and client
export type {
  InferSchema,
  ExtractParams,
  CollectionDef,
  StreamDef,
  RouterDef,
  SubscriptionHandle,
  CollectionClient,
  StreamClient,
  RpcClient,
} from "./types"
