// Shared types — importable from both server and client
export type {
  InferSchema,
  ExtractParams,
  CollectionDef,
  StreamDef,
  Router,
  RouterDef,
  SubscriptionHandle,
  CollectionClient,
  StreamClient,
  RpcClient,
} from "./types"
