{
  "name": "@ellie/hindsight-eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check-types": "tsgo --noEmit",
    "test": "bun test",
    "eval:baseline": "bun run scripts/run-baseline.ts",
    "eval:baseline:repro": "bun run scripts/repro-check.ts",
    "eval:phase2": "bun run scripts/run-phase2-verification.ts",
    "eval:phase2:repro": "bun run scripts/phase2-repro-check.ts"
  },
  "dependencies": {
    "@ellie/hindsight": "workspace:*",
    "@ellie/utils": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.0.0"
  }
}
