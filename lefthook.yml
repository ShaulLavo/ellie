pre-commit:
  parallel: false
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxfmt --check {staged_files}
    typecheck:
      run: bun run check-types
    lint:
      run: bun run lint
    test:
      run: bun run test
